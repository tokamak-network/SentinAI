# Ralph Progress Log
Started: 2026-03-08
Branch: ralph/proposal-30-deployment-hardening
---

## 2026-03-08 17:41 - US-01~05 (Custom EVM Client Profile via JSON)
- Implemented all 5 user stories in one session after context compaction
- US-01: src/lib/client-profile/custom-profiles.ts — loadCustomProfiles, getCustomProfile, mergeWithBuiltins; 10 tests
- US-02: Updated normalizeExecutionFamily in client-detector.ts to check custom profiles; updated resolveClientProfile to use mergeWithBuiltins; added ethrex + unknown fallback tests
- US-03: POST /api/v2/client-detect route; handles 400 (missing rpcUrl), 200 (detection data), 500 (errors); 5 tests
- US-04: CustomClientSection added to website/setup page; shows only when clientFamily === 'other'; has client name input + auto-detect button + result display + error display
- US-05: generateSetupScript now writes client-profiles.json heredoc when customProfile is defined
- Files changed: src/lib/client-profile/custom-profiles.ts, src/lib/__tests__/custom-profiles.test.ts, src/lib/client-detector.ts, src/lib/client-profile/env-overrides.ts, src/lib/__tests__/client-detector.test.ts, src/app/api/v2/client-detect/route.ts, src/app/api/v2/client-detect/route.test.ts, website/src/app/setup/page.tsx, website/src/lib/generate-setup-script.ts
- **Learnings for future iterations:**
  - vi.mock('fs') must be called before module imports to work with fs.readFileSync mocks in vitest
  - The `health/route.test.ts` failure is pre-existing (chain plugin mock issue) — not our concern
  - website/ needs its own `npx tsc --noEmit` check (separate tsconfig)
  - generate-setup-script.ts uses template literals for shell heredocs — keep SENTINAI_PROFILES_EOF as delimiter (different from SENTINAI_EOF used for .env.local)
  - When the linter removes imports, check `grep` to confirm imports are actually present after stash pop
---
