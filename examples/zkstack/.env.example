# ============================================================
# SentinAI x ZK Stack (Template)
# ============================================================

# --- Required ---
CHAIN_TYPE=zkstack
L2_RPC_URL=http://localhost:3050

# --- ZK Stack mode ---
ZKSTACK_MODE=legacy-era
# core-only | full (default: core-only in docker mode)
ZKSTACK_COMPONENT_PROFILE=core-only

# --- L1 RPC ---
L1_RPC_URLS=http://localhost:8545

# --- Optional settlement/proof probes ---
# Settlement card is shown only when this is set.
ZK_BATCHER_STATUS_URL=http://localhost:8081/status/settlement
# Proof card is shown only when this is set.
# ZK_PROOF_RPC_URL=http://localhost:3320/proof/status

# --- Settlement metadata ---
ZK_SETTLEMENT_LAYER=l1
ZK_FINALITY_MODE=confirmed

# --- Local orchestrator mode (optional) ---
# k8s | docker
ORCHESTRATOR_TYPE=docker
# Use this only in docker mode.
# Path must point to a core-only server-v2 compose (see examples/zkstack/docker-compose.core-only.yml).
DOCKER_COMPOSE_FILE=/absolute/path/to/docker-compose.core-only.yml

# --- ZK Stack docker service mapping (docker mode only) ---
# server-v2 topology often runs batcher pipeline inside core service.
ZKSTACK_EXECUTION_SERVICE=zkstack-core
ZKSTACK_BATCHER_SERVICE=zkstack-core
# Core-only template does not run dedicated prover containers.
ZKSTACK_PROVER_SERVICE=zkstack-core

# --- Dashboard ---
NEXT_PUBLIC_NETWORK_NAME=ZK Stack Local L2
